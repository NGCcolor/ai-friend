# 智能旅游 AI 管家 — 项目运行全流程详解

## 一、项目总体架构

```
用户 (Streamlit UI)
    │
    ▼
┌─────────────────────────────────────────────────────┐
│                  LangGraph 状态机                     │
│                                                       │
│  START → Gateway → [chitchat] → END (直接回复)        │
│                → [tool_only] → ToolExecutor → END     │
│                → [planning]  → Memory → Retriever     │
│                                    → PlanAgent        │
│                                    → AuditAgent       │
│                                    → (Pass) END       │
│                                    → (Fail) PlanAgent │
│                                    → (熔断) END       │
└─────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────┐
│               ChromaDB 向量数据库                      │
│                                                       │
│  travel_user_profiles  (用户画像，按 user_id 隔离)     │
│  travel_history_records (对话历史，按 user_id 隔离)     │
│  public_knowledge      (公共知识 + UGC评价，全局共享)   │
└─────────────────────────────────────────────────────┘
```

---

## 二、配置系统

### 2.1 配置加载流程

```
settings.yaml  →  config.py (Settings类)  →  全局单例 settings
```

`config.py` 中的 `Settings` 类在模块导入时读取 `settings.yaml`，通过 `__getattr__` 支持链式访问：

```python
settings = Settings()                        # 模块级单例
settings.model.chat_model_name               # → "mimo-v2.5-pro"
settings.chroma.persist_directory            # → "chroma_db"
```

内部通过 `ConfigSection` 代理类实现递归包装，支持 `settings.model.chat_api_key` 这种多级访问。

### 2.2 配置结构

| 节点 | 用途 | 关键字段 |
|------|------|----------|
| `model` | 模型配置 | `chat_model_name`(mimo-v2.5-pro), `chat_api_key`, `chat_base_url`, `embedding_model_name`(text-embedding-v4), `embedding_api_key` |
| `agent` | Agent 外部资源 | `heweather_key`(和风天气), `amap_key`(高德地图) |
| `chroma` | 向量数据库 | 三个 collection 名, `persist_directory`, `k=3`, `chunk_size=200`, `chunk_overlap=20` |
| `prompts` | Prompt 文件路径 | 6 个 `.txt` 文件路径 |
| `user_db` | 用户认证存储 | `dir`, `filename` |

### 2.3 模型工厂

`model/factory.py` 使用抽象工厂模式，模块导入时创建两个全局单例：

```python
# 聊天模型 — 小米 Mimo，OpenAI 兼容协议
chat_model = ChatOpenAI(
    model="mimo-v2.5-pro",
    api_key="tp-xxx",
    base_url="https://token-plan-cn.xiaomimimo.com/v1",
    temperature=0.7,
    extra_body={"enable_thinking": False},  # 关闭 MIMO 的思考模式
)

# 嵌入模型 — 阿里 DashScope
embed_model = DashScopeEmbeddings(
    model="text-embedding-v4",
    dashscope_api_key="sk-xxx"
)
```

---

## 三、用户认证与会话管理

### 3.1 认证流程

```
app.py
├── 未登录 → 登录/注册页面
│   ├── 登录: load_users() → 校验 MD5(hash) → 设置 session_state → st.rerun()
│   └── 注册: 校验 → 生成 uid_{timestamp} → save_users() → JSON 文件
│
└── 已登录 → 主聊天界面
    ├── sidebar: 用户信息 + 技能列表 + 退出/清空按钮
    └── 主区域: 消息渲染 + 输入处理
```

用户数据存储在 `data/users_db.json`，结构：

```json
{
  "username": {
    "user_id": "uid_1716100000",
    "password": "5d41402abc4b2a76b9719d911017c592"  // MD5
  }
}
```

### 3.2 会话状态

Streamlit 的 `st.session_state` 管理以下状态：

| Key | 类型 | 说明 |
|-----|------|------|
| `logged_in` | bool | 是否已登录 |
| `username` | str | 用户名 |
| `user_id` | str | 用户唯一 ID |
| `message` | list[dict] | 聊天历史 `[{role, content}]` |
| `agent` | ReactAgent | Agent 单例（登录后首次创建） |

---

## 四、短期记忆的组装

### 4.1 原理

短期记忆解决大模型的"金鱼脑"问题——让模型能理解"那里"、"上次说的"、"明天"等指代。

### 4.2 实现方式

在 `app.py` 中，用户发送消息后：

```python
# 取最近 3 轮对话（6 条消息），排除当前用户输入
recent_msgs = st.session_state["message"][:-1][-6:]

history_str = ""
for msg in recent_msgs:
    role_name = "用户" if msg["role"] == "user" else "AI管家"
    history_str += f"{role_name}：{msg['content']}\n"
```

组装后的 `history_str` 示例：

```
用户：我想去哈尔滨玩
AI管家：哈尔滨是个好选择！你想玩几天？
用户：3天吧，帮我规划一下
```

这个字符串作为 `short_term_history` 参数传入 `ReactAgent.execute_stream()`。

### 4.3 短期记忆 vs 长期记忆

| 维度 | 短期记忆 | 长期记忆 |
|------|----------|----------|
| 存储位置 | Streamlit session_state | ChromaDB travel_history_records |
| 生命周期 | 页面刷新即丢失 | 永久存储 |
| 容量 | 最近 6 条消息 | 无限 |
| 用途 | Gateway 指代消解、参数补全 | Retriever 语义召回历史偏好 |
| 传递方式 | `short_term_history` 参数 | `history_db.recall_history()` |

---

## 五、Gateway 意图路由

### 5.1 结构化输出

Gateway 使用 Pydantic 强制 LLM 输出结构化 JSON：

```python
class GatewayOutput(BaseModel):
    intent_type: Literal["chitchat", "tool_only", "planning"]
    chitchat_reply: str          # 闲聊回复
    true_intent: str             # 补全后的真实意图
    rag_keyword: str             # 内部知识库检索关键词
    mcp_keyword: str             # 外部检索关键词
    tool_name: str               # 工具类意图时指定工具名
    tool_args: Dict[str, Any]    # 工具参数
    has_profile_update: bool     # 是否更新画像
    extracted_profile: str       # 提取的画像内容
    is_review: bool              # 是否是评价
    review_target: str           # 评价目标
    review_rating: float         # 评价星级
    review_content: str          # 评价内容
```

### 5.2 意图分类逻辑

Gateway 的 system prompt 明确要求 LLM 结合历史对话进行意图分类：

| 意图类型 | 触发条件 | 处理路径 |
|----------|----------|----------|
| `chitchat` | 打招呼、闲聊、评价反馈 | 直接返回 `chitchat_reply`，结束 |
| `tool_only` | 天气、时间、定位等纯工具调用 | ToolExecutor 执行工具，结束 |
| `planning` | 旅游规划需求 | Memory → Retriever → PlanAgent → AuditAgent |

### 5.3 历史参数补全

**场景示例**：用户之前说"我在北京"，现在说"明天天气"

1. Gateway 收到 `query="明天天气"` + `history="用户：我在北京\n..."`
2. LLM 识别出 `tool_only` + `tool_name="weather_query_skill"` + `tool_args={"city": "北京"}`
3. 如果 LLM 漏了 city，`_validate_tool_args` 会从历史中程序化提取城市
4. 如果还是找不到，降级为 `planning` 意图

**城市提取算法** (`_extract_city_from_history`)：
- 维护一个 48 个城市的硬编码列表
- 先从当前 query 匹配
- 再从历史对话倒序扫描（优先最近的对话）

### 5.4 画像回写

Gateway 同时承担两个副任务：
- **画像更新**：`has_profile_update=True` 时，调用 `profile_db.upsert_profile()` 写入 ChromaDB
- **UGC 评价回写**：`is_review=True` 时，调用 `public_db.add_review()` 写入公共知识库

---

## 六、Memory 节点 — 结构化记忆提取

### 6.1 作用

将用户的自然语言需求 + 长期画像，转换为结构化的旅行档案（`travel_spec`）。

### 6.2 输入

```
用户长期画像: profile_db.get_user_profiles(user_id)  ← 从 ChromaDB 语义检索
旧记忆档案:   state["travel_spec"]                    ← 上一轮的 travel_spec
用户最新输入: state["query"]
```

### 6.3 结构化输出

```python
class TravelMemory(BaseModel):
    destination: str          # 目的地
    duration: str             # 天数
    companions: str           # 同行人
    budget: str               # 预算
    special_requests: str     # 忌口、避雷等核心约束
```

### 6.4 三级降级策略

```
structured_output (Pydantic 强制格式)
    ↓ 失败
raw LLM + regex r'\{[^{}]+\}' 提取 JSON
    ↓ 失败
返回旧 travel_spec 不变
```

这个模式在项目中出现三次：Gateway、Memory、AuditAgent，确保任何情况下都不会崩溃。

---

## 七、Retriever 节点 — 信息分层召回

### 7.1 三层信息架构

Retriever 是信息聚合中心，从三个数据源强制召回事实：

```
facts_context = 专属画像 + 历史偏好 + 公共事实 + 外部动态(可选)
```

| 层级 | 数据源 | 方法 | 说明 |
|------|--------|------|------|
| 内部画像 | `travel_user_profiles` | `profile_db.get_user_profiles(user_id)` | 用户忌口、体力、预算偏好 |
| 内部历史 | `travel_history_records` | `history_db.recall_history(user_id, rag_keyword)` | 语义检索过去对话 |
| 内部公共 | `public_knowledge` | `public_db.search_knowledge(rag_keyword)` | 旅游攻略 + UGC 评价 |
| 外部动态 | AgentExecutor 动态调用 | 天气、定位等工具 | 仅当 `mcp_keyword` 非空时触发 |

### 7.2 ChromaDB 三表隔离

```
┌─────────────────────────────────────────────────┐
│                 ChromaDB (chroma_db/)             │
│                                                   │
│  travel_user_profiles                             │
│  ├── metadata: {user_id, type: "profile"}         │
│  ├── filter: $and: [{user_id}, {type: "profile"}] │
│  └── query: "用户的核心旅游偏好、忌口、住宿要求"    │
│                                                   │
│  travel_history_records                           │
│  ├── metadata: {user_id, type: "chat_history"}    │
│  ├── filter: $and: [{user_id}, {type: "chat_history"}] │
│  └── query: rag_keyword（语义匹配）               │
│                                                   │
│  public_knowledge                                 │
│  ├── metadata: {type: "ugc_review", target, ...}  │
│  ├── filter: 无（全局共享）                        │
│  └── query: rag_keyword（语义匹配）               │
└─────────────────────────────────────────────────┘
```

用户画像和对话历史通过 `user_id` 物理隔离，不同用户的数据互不可见。公共知识库全局共享。

### 7.3 外部动态调用（MCP 子 Agent）

当 `mcp_keyword` 非空时，Retriever 会创建一个临时的 `AgentExecutor`：

```python
agent = create_tool_calling_agent(chat_model, self.all_tools, prompt)
agent_executor = AgentExecutor(agent=agent, tools=self.all_tools, max_iterations=3)
mcp_res = agent_executor.invoke({"keyword": mcp_keyword})
```

这是一个"Agent 中套 Agent"的模式——外层是 LangGraph 状态机，内层是 LangChain AgentExecutor，由 LLM 自主决定调用哪个工具。

---

## 八、PlanAgent — 路书生成

### 8.1 角色定义

```
角色: 金牌旅游规划师
职责: 只写路书，不质检
模型: 小米 Mimo (mimo-v2.5-pro)
```

### 8.2 输入组装

```python
system_msg = "你是一个金牌旅游规划师。请严格结合事实清单和用户核心档案撰写详细、可执行的旅行路书。"

user_msg = f"""
【核心档案(红线)】: {travel_spec}     ← Memory 节点输出的结构化档案
【真实意图(目标)】: {true_intent}      ← Gateway 补全后的完整意图
【客观事实(依据)】: {facts_context}    ← Retriever 聚合的三层事实
"""

# 如果有审计反馈，追加在最前面
if feedback:
    user_msg = f"【请针对以下审计意见进行针对性修改】：\n{feedback}\n\n" + user_msg
```

### 8.3 输出

返回自由文本格式的旅行路书，同时递增 `revision_count`。

---

## 九、AuditAgent — 质量审计

### 9.1 角色定义

```
角色: 严苛审计官
职责: 只质检挑错，不写路书
模型: 小米 Mimo (mimo-v2.5-pro)
```

### 9.2 审计维度

| 审计项 | 对照基准 | 检查内容 |
|--------|----------|----------|
| 红线合规 | `travel_spec` | 是否违反用户设定的忌口、预算、天数等硬约束 |
| 目标达成 | `true_intent` | 是否覆盖了用户的真实需求 |
| 事实准确 | `facts_context` | 引用的信息是否与检索到的事实一致 |

### 9.3 结构化输出

```python
class AuditResult(BaseModel):
    status: str        # "Pass" 或 "Fail"
    error_type: str    # 错误分类
    audit_report: str  # 问题描述
    suggestions: str   # 修改建议
```

### 9.4 降级策略

```
structured_output (Pydantic 强制格式)
    ↓ 失败
raw LLM + regex r"\{[^{}]+\}" 提取 JSON
    ↓ 失败
返回默认 AuditResult(status="Pass")  ← 宁可放行，不让系统卡死
```

---

## 十、双 Agent 协作循环与熔断机制

### 10.1 循环流程

```
PlanAgent (第1次起草)
    │
    ▼
AuditAgent (第1次审计)
    ├── Pass → END (输出路书)
    └── Fail → 反馈给 PlanAgent
                │
                ▼
          PlanAgent (第1次修改，revision_count=2)
                │
                ▼
          AuditAgent (第2次审计，audit_count=2)
                ├── Pass → END (输出路书)
                └── Fail → 触发熔断 → END (强制输出最后一版)
```

### 10.2 熔断条件

```python
def route_after_audit(state):
    if state["eval_status"] == "Pass":
        return END                                    # 审计通过
    if state["revision_count"] >= 2 or state["audit_count"] >= 2:
        return END                                    # 强制熔断
    return "PlanAgent"                                # 继续修改
```

- **revision_count >= 2**：PlanAgent 最多改 2 次
- **audit_count >= 2**：AuditAgent 最多审 2 次
- 任一条件触发即熔断，输出最后一版路书
- LangGraph 的 `recursion_limit=15` 是兜底保护

### 10.3 为什么不让两个 Agent 无限循环？

1. **成本控制**：每次循环消耗 2 次 LLM 调用（Plan + Audit）
2. **体验保障**：用户等太久会流失
3. **收益递减**：第 3 轮以后的修改通常只是微调，边际收益很低
4. **死循环风险**：两个 Agent 可能互相挑刺，永远不 Pass

---

## 十一、工具系统

### 11.1 工具注册表

`SkillManager` 使用注册表模式管理所有工具：

```python
skill_manager = SkillManager()
skill_manager.register_skill(WeatherQuerySkill())      # 天气
skill_manager.register_skill(TimePerceptionSkill())     # 时间
skill_manager.register_skill(LocationPerceptionSkill()) # 定位
skill_manager.register_skill(TransitRouteSkill())       # 公交路线
skill_manager.register_skill(UpdateUserProfileSkill())  # 画像更新
skill_manager.register_skill(FindSimilarVibeSkill())    # 相似用户
skill_manager.register_skill(GetUserProfileSkill())     # 画像读取
skill_manager.register_skill(SaveGlobalFeedbackSkill()) # 全局反馈
skill_manager.register_skill(RagSkill())                # RAG 检索
```

### 11.2 工具调用重试

```python
def retry_tool_call(func, args=None, max_retries=2):
    for attempt in range(max_retries + 1):
        try:
            return func(args or {})
        except Exception as e:
            if attempt < max_retries:
                time.sleep(0.5)   # 重试间隔 0.5 秒
    raise last_error              # 最终失败则抛出异常
```

最多 3 次尝试（1 次初始 + 2 次重试），用于网络请求类工具（天气 API、高德 API）的容错。

### 11.3 核心工具详情

| 工具名 | 参数 | 数据源 | 返回 |
|--------|------|--------|------|
| `weather_query_skill` | `city: str` | 高德天气 API | 天气、温度、风向、风力 |
| `time_perception_skill` | 无 | 系统时间 | 日期、时间、星期、季节 |
| `location_perception_skill` | 无 | 高德 IP 定位 + 逆地理编码 | 详细地址 |
| `transit_route_skill` | `origin, destination, city` | 高德公交路线 API | 距离、时长、换乘步骤 |
| `rag_summarize` | `query: str` | ChromaDB + HyDE | RAG 摘要 |

---

## 十二、向量检索与 HyDE

### 12.1 HyDE（假设性文档嵌入）

HyDE 的核心思想：用 LLM 生成一个"假设性回答"，用它代替原始 query 做向量检索。

```
用户 query: "哈尔滨3天攻略"
    │
    ▼
LLM 生成假设性文档:
"哈尔滨是东北著名冰雪城市，中央大街是必去景点，圣索菲亚教堂是地标建筑，
 冬季可去冰雪大世界，推荐老昌春饼、马迭尔冰棍..."
    │
    ▼
用假设性文档做 similarity_search（比原始 query 匹配到更多相关文档）
    │
    ▼
score_threshold=0.8 过滤，k=3 取 top3
```

### 12.2 为什么 HyDE 有效？

原始 query "哈尔滨3天攻略" 只有 5 个语义单元。假设性文档包含了大量地名、景点、美食等实体，大幅扩展了语义向量空间，提高了召回率。

### 12.3 检索流程

```python
def retriever_docs(query, use_hyde=True):
    if use_hyde:
        query = _generate_hyde_document(query)  # LLM 生成假设性文档
    retriever = vector_store.as_retriever(
        search_type="similarity_score_threshold",
        search_kwargs={"score_threshold": 0.8, "k": 3}
    )
    docs = retriever.invoke(query)
    if not docs:
        return "未检索到高置信度参考信息"
    return "\n\n".join(doc.page_content for doc in docs)
```

---

## 十三、流式输出与打字机效果

### 13.1 流式接口

`ReactAgent.execute_stream()` 是一个 Python 生成器，通过 `yield` 逐块输出：

```python
def execute_stream(self, query, user_id, short_term_history):
    for event in self.app.stream(initial_state, {"recursion_limit": 15}):
        for node_name, state_update in event.items():
            if node_name == "Gateway" and state_update.get("is_chitchat"):
                yield state_update["chitchat_reply"]
                return
            if node_name == "ToolExecutor":
                yield state_update["chitchat_reply"]
                return
            if node_name == "PlanAgent":
                current_draft = state_update["draft_itinerary"]
            if node_name == "AuditAgent" and state_update["eval_status"] == "Pass":
                yield current_draft
                return
    # 熔断兜底
    yield current_draft
```

### 13.2 打字机效果

在 `app.py` 中，通过逐字符 yield 模拟打字机效果：

```python
def capture(generator, cache_list):
    for chunk in generator:
        cache_list.append(chunk)
        for char in chunk:
            time.sleep(0.01)  # 每个字符间隔 10ms
            yield char
```

---

## 十四、完整运行时序图

```
用户输入 "帮我规划哈尔滨3天，不爱吃辣"
    │
    ▼
[app.py] 组装短期记忆 history_str (最近6条)
    │
    ▼
[ReactAgent.execute_stream] 记录到 history_db
    │
    ▼
[Gateway] LLM 分类 → intent_type="planning", true_intent="哈尔滨3天旅游规划，不吃辣"
    │
    ▼
[Memory] 读取 profile_db → 结构化 travel_spec:
    {destination: "哈尔滨", duration: "3天", companions: "未知", budget: "未知", special_requests: "不吃辣"}
    │
    ▼
[Retriever] 三源召回:
    ├── profile_db: "用户偏好：不吃辣，喜欢自然风光"
    ├── history_db: "用户上次聊过哈尔滨冰雪大世界"
    ├── public_db: "哈尔滨冬季攻略...中央大街美食..."
    └── AgentExecutor: weather_query_skill(city="哈尔滨") → "-15℃, 大雪"
    │
    ▼
[PlanAgent] 第1次起草 → 输出完整路书初稿
    │
    ▼
[AuditAgent] 第1次审计
    ├── 检查：是否包含辣味推荐？→ 发现推荐了"老昌春饼配辣酱"
    └── 结果: Fail, feedback="违反忌口约束：推荐了辣酱"
    │
    ▼
[PlanAgent] 第1次修改 → 去掉辣酱推荐，替换为其他菜品
    │
    ▼
[AuditAgent] 第2次审计
    └── 结果: Pass ✓
    │
    ▼
输出最终路书 + "路书已备好！体验完回来记得告诉我感受"
    │
    ▼
[app.py] 打字机效果渲染，存入 session_state["message"]
```

---

## 十五、项目文件结构

```
pythonProject1/
├── app.py                          # Streamlit 入口，用户认证 + 会话管理
├── config.py                       # Settings 类，配置加载
├── settings.yaml                   # 统一配置文件
├── model/
│   └── factory.py                  # 模型工厂，chat_model / embed_model 单例
├── agent/
│   ├── state.py                    # TravelState TypedDict
│   ├── react_agent.py              # LangGraph 状态机 + 6 个节点
│   ├── plan_agent.py               # PlanAgent（金牌规划师）
│   ├── audit_agent.py              # AuditAgent（严苛审计官）
│   ├── rag/
│   │   ├── base_chroma.py          # ChromaDB 基类
│   │   ├── history_record.py       # 对话历史存储
│   │   ├── user_profile.py         # 用户画像存储
│   │   └── public_knowledge.py     # 公共知识 + UGC 评价
│   ├── skills/
│   │   ├── __init__.py             # 工具注册
│   │   ├── skill_manager.py        # 注册表管理器
│   │   ├── context/                # 时间、定位感知
│   │   ├── external/               # 天气、路线、小红书(已禁用)
│   │   └── profile/                # 画像读写、相似用户
│   └── tools/
│       └── middleware.py           # 工具调用中间件
├── rag/
│   ├── vector_store.py             # 向量存储服务（技能层使用）
│   └── rag_service.py              # RAG 摘要服务（含 HyDE）
├── prompts/                        # Prompt 模板文件
├── utils/
│   ├── gateway_handler.py          # Gateway 意图分类
│   ├── logger_handler.py           # 日志
│   ├── config_handler.py           # 向后兼容配置桥
│   └── prompt_loader.py            # Prompt 文件加载器
└── data/
    └── users_db.json               # 用户认证数据库
```
